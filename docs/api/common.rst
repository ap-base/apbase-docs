apbase.config
=============

Process-wide runtime settings used by the map pipeline.

.. autodata:: apbase.config
   :annotation:

   Global configuration object. The most common setting is
   ``apbase.config["n_threads"]``.
